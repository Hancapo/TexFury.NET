<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>TexFury</RootNamespace>
    <AssemblyName>TexFury</AssemblyName>
    <Version>1.6.0</Version>
    <Authors>Hancapo</Authors>
    <Description>Fast image-to-DDS conversion and RAGE texture dictionary toolkit</Description>
    <PackageId>TexFury</PackageId>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageTags>dds;texture;bc7;compression;ytd;wtd;mipmap;rdr2;gta</PackageTags>
    <RepositoryUrl>https://github.com/Hancapo/TexFury.NET</RepositoryUrl>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="runtimes\win-x64\native\texfury_native.dll">
      <Pack>true</Pack>
      <PackagePath>runtimes\win-x64\native</PackagePath>
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="runtimes\win-x64\native\oo2core_5_win64.dll">
      <Pack>true</Pack>
      <PackagePath>runtimes\win-x64\native</PackagePath>
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

</Project>
